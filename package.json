{
  "name": "terminal-speedtest",
  "version": "0.1.0",
  "description": "Terminal-inspired speed and packet-loss test for web and Android (HTML5 + Capacitor), with a self-hosted Node measurement server.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "node server/server.js",
    "android:sync": "cap sync android",
    "android:open": "cap open android"
  },
  "engines": {
    "node": ">=18.20.0"
  },
  "dependencies": {
    "node-datachannel": "^0.33.4"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  }
}
